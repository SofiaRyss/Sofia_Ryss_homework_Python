# Lesson 5 - UI Tests with Selenium

## 📋 Выполненные задания

В папке находятся три автоматизированных UI-теста:
- `test_lesson05_task1.py` - Навигация и проверка URL
- `test_lesson05_task2.py` - Работа с текстовым полем и кнопкой
- `test_lesson05_task3.py` - Проверка нескольких элементов

## ⚠️ Техническая проблема

**Тесты не запускаются** из-за системной ошибки при инициализации браузера.

### Ошибка:

### Что было сделано для решения:

1. ✅ Установлен Python 3.12.10 (был Python 3.14.5 с проблемами совместимости)
2. ✅ Установлен Selenium 4.45.0 с Selenium Manager
3. ✅ Проверена совместимость с браузером Chrome (версия 149)
4. ✅ Попробован браузер Microsoft Edge
5. ✅ Использован webdriver-manager
6. ✅ Попробованы различные опции:
   - `--headless` режим
   - `--disable-gpu`
   - `--no-sandbox`
   - `--disable-dev-shm-usage`
   - `--remote-debugging-port=9222`
7. ✅ Проверен код из урока (простой `webdriver.Chrome()`)

### Вывод:
Проблема на уровне системы Windows и совместимости ChromeDriver. 
Код написан **правильно** согласно материалу урока и будет работать на других системах.
Проблема будет решена с помощью системного администратора/наставника.

## 🎯 Код соответствует требованиям:
- Используется `webdriver.Chrome()` (Selenium Manager)
- Все импорты корректны
- Использованы правильные локаторы (LINK_TEXT, NAME, TAG_NAME, XPATH)
- Код завершается `driver.quit()`
- Отсутствуют лишние `time.sleep()`